注意了，有时候在使用某个函数中的if去判别参数时，为了根据不同类型去执行不同语句，容易将传入的参数类型弄混淆，比如对象和字符串类型混淆

比如 错误示例
var b={...}
var c={...}
function(a){ if(a=='b'){...}else if(a=='c'){...} }

之前的点击按钮后，动画切换依然会自动进行的bug修复了，为了避免再次出现bug，延缓自动切换的计时器时间最好不要设定的大于3秒